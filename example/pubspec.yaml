name: example
description: Example usage of component_generator code generation
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  component_generator:
    path: ../
  component_set: ^0.1.2

dev_dependencies:
  build_runner: ^2.7.1

dependency_overrides:
  component_set:
    path: ../../component
