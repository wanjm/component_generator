1. 自动检查返回值;
2. 添加函数去封装报文;(返回string和content-type);
3. client的默认处理函数;
4. 每个接口有自己的预处理返回值的权限;
5. 添加失效检查接口;
6. buffer接口,需要有机会清空自己的缓存的promise的机会. 否则后续有好多活挂在哪里;
7. 解决各个异常的处理情况；(开始时要禁止用户再次点击(直到失败或成功), 通过showBusy的方法可以避免,最好先出现遮挡,然后再出现转圈);

完成:
8. 通过下载http大叶面测试http2的data拼接问题；



